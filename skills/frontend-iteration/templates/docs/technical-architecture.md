# 技术架构

<!-- 描述项目开发环境、目标平台、工程约定与测试配置，保持简洁。 -->

## 项目

- 目标平台：
- 包管理器：
- 运行时：
- 构建工具：
- 框架：
- UI 库：
- 状态管理：
- 路由：
- API 客户端：

## 开发与命令

<!-- 示例： -->

| 任务 | 命令 | 备注 |
|------|------|------|
| 安装依赖 | `...` | |
| 开发服务 | `...` | |
| 构建 | `...` | |
| 代码检查 | `...` | |
| 类型检查 | `...` | |
| ... | ... | |

## 目录约定

<!-- 示例： -->

| 区域 | 路径 | 备注 |
|------|------|------|
| 页面/路由 | `...` | |
| 组件 | `...` | |
| Hooks | `...` | |
| API | `...` | |
| 类型 | `...` | |
| 测试 | `...` | 见下[文件位置](#文件位置) |
| ... | ... | |

## 代码风格

<!-- 示例，约定的描述须清晰可执行： -->

| 维度 | 约定 |
|------|------|
| 命名 | 文件、组件、变量、类型、测试文件 |
| 导入 | 顺序、是否自动导入、路径别名、禁止相对路径层级 |
| 组件 | 函数式/选项式、props 命名、文件结构 |
| 状态 | 局部 vs store、何时抽 hook |
| API 调用 | 仅通过 `src/api/` 封装，禁止页面直接 `fetch` |
| 错误处理 | 统一模式（toast / inline / reject） |
| 注释与 TODO | 接口待定用 `TODO(vX.Y.Z): 接口联调待定`（见 `frontend-iteration/references/api-integration-guide.md`） |
| 检查与格式化 | 与「开发与命令」表对应 |
| ... | ... |

## 测试

### 框架

| 层级 | 框架 | 版本 | 备注 |
|------|------|------|------|
| 单元 | | | |
| 集成 | | | |
| E2E | | | |

### 命令

| 层级 | 命令 | 备注 |
|------|------|------|
| 单元 | `...` | |
| 集成 | `...` | |
| E2E | `...` | |
| 全量 | `...` | |

### 文件位置

<!--

具体文件路径需要遵守：

1.  本版本路径需要带有版本号：
    1. `src/**/<name>.vX.Y.Z.test.unit.ts`
    2. `tests/unit/vX.Y.Z/<name>.test.ts`
    3. `tests/fixtures/vX.Y.Z/<name>.ts`
    4. ...
2. 跨版本路径无需版本号：
    1. `src/**/<name>.test.unit.ts`
    2. `tests/unit/<name>.test.ts`
    3. `tests/fixtures/<name>.ts`
    4. ...

-->

| 层级 | 路径 | 备注 |
|------|------|------|
| 单元 | `...` | 本版本单测 |
| 集成 | `...` | 本版本集成测试 |
| E2E | `...` | 本版本 E2E 测试 |
| 共用夹具/Mock | `...` | 跨版本共用 |
| 夹具/Mock | `...` | 仅被本版本使用 |

### 项目约定

- Mock 策略：
- 测试数据/夹具：
- 稳定选择器：
- 网络拦截：
- 可访问性要求：

## 框架/库 API 用法

需要了解框架/库 API 用法或行为时：

1. 优先读项目内既有代码
2. 网络搜索对应版本官方框架/库文档
3. 遵循本文件声明的版本
4. 不凭过期记忆写 API 细节

## 备注

- …
